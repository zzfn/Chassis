services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: chassis
      POSTGRES_USER: chassis
      POSTGRES_PASSWORD: chassis
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chassis"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
